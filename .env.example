# Google Cloud Configuration
GCP_PROJECT_ID=your-gcp-project-id
GCP_LOCATION=us-central1

# LLM Model (gemini-2.5-flash is faster, gemini-2.5-pro is more capable)
LLM_MODEL=gemini-2.5-flash

# Path to gcloud credentials directory
# Default: ~/.config/gcloud
GCLOUD_CONFIG_PATH=~/.config/gcloud

# PostgreSQL (defaults are set in docker-compose.yml)
# PGHOST=pgvector
# PGPORT=5432
# PGDATABASE=codebase_rag
# PGUSER=postgres
# PGPASSWORD=postgres
